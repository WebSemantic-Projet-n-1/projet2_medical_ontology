services:
  reasoner:
    build:
      context: ..
      dockerfile: analyse/Dockerfile
    volumes:
      # Données OWL Gene Ontology
      - ../data:/app/data:ro
      # Résultats (JSON, Markdown)
      - ./results:/app/analyse/results

