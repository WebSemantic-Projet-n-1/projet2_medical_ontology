/**
 * Popup : configuration et enregistrement des options.
 * - Domaine (domainId)
 * - URL du service web (apiBaseUrl)
 * - Cache on/off (cacheEnabled)
 * - Afficher les stats d’utilisation (nb requêtes en cache, etc.) si stockées.
 */

// TODO: au chargement, lire chrome.storage.sync ou local et remplir les champs
// TODO: bouton Enregistrer : sauvegarder les valeurs dans chrome.storage
// document.getElementById("save").addEventListener("click", () => { ... });
