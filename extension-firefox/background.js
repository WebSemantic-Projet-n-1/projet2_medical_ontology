/**
 * Partie 3 - Background script Firefox (Manifest V2).
 * Même rôle que extension-chrome/background.js ; pas de service worker, script persistant.
 * Utiliser browser.runtime.onMessage (ou chrome.runtime) et même logique de cache / API.
 */

// TODO: même logique que Chrome ; adapter si besoin pour browser.* vs chrome.*
